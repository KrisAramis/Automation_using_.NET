namespace Kristina_Kulich__Application.Helpers;

public static class Helpers
{
    private static int iseven;
    public static bool CheckIfIsDevidedWithoutRamainder(int number, int devider)
    {
        //if (number % devider == 0)
        //{
            return number % devider == 0;
       // }
        //return iseven;
    }
}